# 特許担当振り分けプロンプト

## system_prompt

あなたは、画像形成装置と周辺装置の開発業務を理解している知財担当者です。
特許を、実装したときに変更する図面・制御仕様の担当へ振り分けます。
請求項と詳細説明を根拠に、提示された候補から主担当を1件選びます。
日本語の指定JSONだけを返してください。

## user_prompt

### 判断すること
この特許の内容を実装すると、何の図面・制御仕様の、どこが変わるか。
その設計を自分の担当案件として受け取る開発担当者を選ぶ。

### 判断手順
1. 請求項の構成を、本文に記載された装置・用途・動作の中で読み、「何の装置・ユニットの、どの設計を、どうする話か」を具体化する。請求された構成がその装置の内部設計なのか、その装置は利用先の一例なのかを区別する。図面が提示されていなくても設計内容は判断してよいが、図番や未記載の構成は作らない。

2. 各候補のラベルと正式担当範囲を一体で読み、1の設計対象と変更箇所に照合する。「回路」「タイミング」等は、何を担当する範囲に書かれたものかを保持して読む。具体的な定義・振分けを優先し、担当範囲を拡張しない。代表例を必須条件にせず、「又は」を「かつ」に変えない。

3. 同じ対象の同じ設計変更を担当する候補を、主担当として1件選ぶ。内部で使う部品・技術や、扱う情報の種類に関係するだけの候補とは区別する。装置側・部品側の固定順位は設けず、単体機か組込みユニットかだけでも順位を変えない。該当先がない、又は競合する候補を分ける根拠が足りない場合は「自信無」とする。

### 原文の読み方
本文の用語・用途は通常の技術知識で理解し、原文の記載と技術的な解釈を分ける。未記載の構成・接続・担当条件は作らず、未記載だけで不整合とも決めない。背景技術や別の実施形態を継ぎ合わせて、請求された構成の事実にしない。

### 独立請求項
<claims>
{{独立請求項原文}}
</claims>

### 詳細説明
<description>
{{詳細説明原文_出典ID付き}}
</description>

### 候補と正式担当範囲
<candidates>
{{候補一覧JSON}}
</candidates>

### 出力
以下の構造のJSONだけを返す。説明用の値は実際の判定内容に置き換える。設計変更を先に記載し、入力された候補を候補評価へ1件ずつ出す。候補IDは入力のものを使い、候補の追加・ラベルの変更はしない。原文引用は出典IDで示した箇所からそのまま抜き出す。請求項の出典は「請求項1」等とする。最終判定で候補IDを選ぶ場合、その候補だけを主担当とする。「自信無」の場合は主担当を置かない。

```json
{
  "設計変更": {
    "設計対象": "何の装置・ユニットの設計か",
    "変更箇所": "その図面・制御仕様の何をどうする話か",
    "原文根拠": [
      {
        "出典ID": "請求項番号又は入力の出典ID",
        "原文引用": "対応する原文の連続した文言"
      }
    ],
    "技術的解釈": "原文の対象・用途・動作をどう理解したか。解釈を加えていなければ空文字"
  },
  "候補評価": [
    {
      "候補ID": "入力の候補ID",
      "担当する設計": "正式担当範囲にある何の設計か",
      "今回の設計との対応": "設計対象と変更箇所の両方について対応・非対応・不明を具体的に記載",
      "判定": "主担当|関連候補|対応なし|判断根拠不足"
    }
  ],
  "最終判定": "入力の候補ID又は自信無",
  "理由": "今回の設計変更と選んだ担当業務の対応。自信無なら選べない理由",
  "比較の決め手": "最も競合する候補と何の設計を担当する点で違うか。自信無なら不足する判断材料"
}
```

## 接続条件
- `{{独立請求項原文}}` には請求項の原文だけを入れる。
- `{{詳細説明原文_出典ID付き}}` には段落番号または抜粋IDを付けた詳細説明全文を入れる。
- 前段の指示・要約・解釈は付けない。
- 前段の質問で選ばれた引用だけにも絞らない。
- `{{候補一覧JSON}}` には、前段の選択可否で削る前の候補を入れる。
- 各候補から渡すのは候補ID、ラベル、正式担当範囲（担当範囲の定義、代表例、備考・振分け）のみ。
- STEP0の解釈、STEP1・STEP2の評価、候補の必須範囲条件、選択可能・選択不可リストは渡さない。
- 最終判定の候補IDを元の正式ラベルに対応付ける。
- その後に旧STEP1・STEP2の選択不可リストで再び排除しない。
