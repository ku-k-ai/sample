# 会社の Codex への引継ぎ：解釈事例の A/B/C 検証

## 依頼

既存の特許分類パイプラインを維持したまま、正式ルールの解釈を「対照事例の参照」で安定させられるか検証する。説明書を追加して終わらず、社内の実装へ最小の評価用アダプターを接続し、許可された実LLMで結果を出す。

**既に2か月・17件で改善と悪化を繰り返している。期限直前。長文注意書きの追加や、正解に合わせた全面改修は今回の仕事ではない。** 成功保証はない。比較の結果、棄却すべきなら棄却を報告する。

## 変更してはいけないもの

発注元の正式ルール（解釈カード経由の実質変更も不可）、既存の段階構成、候補選択資格、モデル・推論設定・プロキシ・API接続、本文を読む段階、既存出力schema。

STEP1が独立項と候補ルールの整合・不足を整理し、STEP2が本文で穴埋めし、最終段がその結果を比較する。B/Cでもこの流れを保持する。**今回の対象特許の全文をSTEP1へ先渡ししない。** STEP2が不要なときの既存スキップは維持する。

## 背景を取り違えない

ユーザーの正解判断には以下がある。ただし、ここに原文や正式な逆側事例はない。以下の記述を全特許へ適用する固定順位にはしない。

- 密着イメージセンサー内部の電源回路の特許を、低圧電源ではなくリーダーへ届ける案件がある。「電源」という手段名と、その案件が何の内部設計かを混同したくない。
- 現像剤を用いて画質を改善する案件を、画像処理でなく現像へ届ける。改良対象と得られる効果の混同を避けたい。
- 印字済み媒体の切断・丁合を、対象物と工程順序から後処理として理解させたい。「画像形成後」という語の明記や特定プリンタとの直結を過剰要求しない。ただし未記載の工程を創作しない。
- 低圧電源は常に最下位、装置側は常に優先、切断は常にオプション、という規則は未承認。そう書けば別件を壊す可能性がある。

この背景は作業方針を理解するためであり、評価対象の正解を分類LLMへ渡すためではない。B/Cで実際に渡す資料は、評価対象を除外した原文付き事例とその派生文だけ。

## 実行順序

### 1. 既存資産を探し、Aを凍結する

ワークスペース内で、17件の入力と正解、各STEPの実際の送受信ログ、正式ルール、呼出し本体、モデル設定を探す。READMEの旧修正を勝手に再適用しない。現在動いているものがA。

`baseline_inventory.md` を社内ローカルに作り、次を記録する。
- 本体の入口・各STEPの実関数・候補IDから正式ラベルへの対応。
- 原文・ルール・プロンプト・schema・実装・モデル設定・キャッシュ方式の場所とハッシュ。
- 既存の正解の出所。発注元確定／ユーザー判定を分ける。新規正解をLLMに付けさせない。
- 原文の不足、正解の衝突、対象候補が開始時から欠ける案件。

17件が足りない場合は、実在した件数を報告する。足りない案件の内容を会話から捏造しない。場所がどうしても分からないものだけ、具体的に質問する。

### 2. 一つだけ実接続を確認し、予算を決める

既存の本番入口と評価アダプターで、同じA入力が同じプロンプト組立・同じ段階構成になることを送信直前ログで確認する。応答の完全一致は要求しない。新しい説明をAに混ぜない。

1回分の実測所要時間・LLM呼出し回数・トークンを取り、ユーザーの締切以内に入る実験予算を設定する。17件×3条件×3反復は153回の**パイプライン実行**であって153回のLLM呼出しではない。勝手に「半日で終わる」と見積もらない。

本番コードは変えず、参照資料を各STEPの補助入力へ差し込むアダプターだけ用意する。既存のschemaに診断欄を増やさず、ログの外側へ計測する。低温度化・新モデル・候補復活などを同時にしない。

### 3. 対照事例を作り、一般化文も同じ材料から作る

`prompts/BUILD_REFERENCE_CARDS.md` と `prompts/BUILD_GENERALIZATIONS.md` を使う。17件から対象を除くと対照が残らない境界は「対照不足」と記す。社内の既存正解例を探し、なければ主実験の転移評価は不足扱い。元事例を言い換えて別特許の例だと扱わない。

資料作成はfold別に行う。生成LLMの入力に、評価対象の原文・正解・その説明・過去誤答を含めない。Codex自身が全件を知っていることは消せないため、作成時の入力ログと来歴を残し、完全な未知評価とは呼ばない。

ユーザーの作業は最小限にする。既に確定した正解は再質問しない。新しい対照例や正式ルールとの衝突など、既存資料では決まらない点だけを短く提示する。LLMの自動承認を発注元承認と偽らない。

### 4. 比較を登録してから実行する

`docs/EXPERIMENT_PROTOCOL.md` に従い、A/B/C、対象、参照、反復、順序、予算、合否条件を結果を見る前に固定する。

初回は、3系統と逆側事例を含む固定pilot（原則最大6件、1反復）で接続と明らかな無効性を確認する。小pilotだけで採用・一般化を断定しない。正例だけを都合よく選ばない。pilotの失敗を見てカードやプロンプトを修正し、同じ実験として再開しない。

予算内で見込みがある場合だけ、固定した17件全体×3条件×3反復へ進む。比較に使用する事例・Cの文章・設定はpilotと同じ版のまま。1対照しかない等の不足は集計から隠さない。

### 5. 最初にずれた段階と、B/Cの違いを報告する

質問の内容変化、事実取得の不足、同じ事実からの条件解釈の差、工程対応の誤り、最終比較の誤りを分ける。モデルが書いた自己説明だけで原因を確定しない。原文と実際の送受信を照合する。

失敗原因を絞れない場合だけ、同じ設定のまま正しい中間事実を固定した診断を行う。正解ラベルは渡さない。これは別run_idの診断で、主実験の精度へ足さない。

## 納品するもの（社内ローカル）

`baseline_inventory.md`、参照カードと来歴、fold別C作成入力、固定マニフェストとハッシュ、実行アダプター、全試行の原送受信ログ、案件別比較、判断根拠の監査、最終 `DECISION.md`。

`DECISION.md` の先頭で「Bを継続検証／Bを棄却／資料・予算不足で判断不能」のいずれかを述べる。BとCの比較、直った案件、壊れた案件、保留、未実行、揺れ、実時間・トークン・既知17件の汚染を記す。単体テスト通過は別欄。

**結果を報告して止める。本番への自動採用、skillsの大量増設、次の無期限プロンプト改善へ進まない。** Bに見込みが出た後に限り、別の承認された作業として事例検索やskills化を検討する。
