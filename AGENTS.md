# このリポジトリでの作業

現在の作業正本は `CODEX_START.md` と `docs/EXPERIMENT_PROTOCOL.md`。
`archive/` は履歴であり、改善指示として自動適用しない。

- 発注元の正式ルールは原文・意味とも変更不可。解釈カードで新しい優先順位を裏から追加しない。
- 既存の STEP1 → STEP2_PLAN → STEP2_REVIEW → FINAL_DECISION を維持する。本文未確認の穴埋め、候補の選択資格、最終比較を飛ばさない。
- 会社の実装・17件・正解・ログを最初に探す。ユーザーに既に説明した分類方針を再度全部書かせない。
- 本番コードを先に直さない。凍結した A/B/C 比較を先に行う。新モデル・RAG検索・FT・skills 自動読込との同時変更禁止。
- 実LLM結果を見た後の試行錯誤を自動ループにしない。採否を報告して止める。
- テスト対象と同一特許・同一ファミリー・派生例・その説明から作った一般化文を参照へ混ぜない。例から得た仮説の出所を偽らない。
- 正解ラベルを推論へ渡さない。別案件の確定正解は事例として使用可。自分の今回の予測を正解化しない。
- 状態を断定文だけで格上げしない。参照例を理由に最終段で候補を復活させない。保留と失敗を隠さない。
- 公開Gitには社内データ、正式ルール、実ケースカード、ログ、鍵を入れない。`.local_eval/` 等はローカル専用。`.gitignore` は公開安全性の保証ではない。`git add .` や自動pushはしない。
- 既存の認証・プロキシ・モデル設定を利用し、鍵を質問文や報告へ貼らない。許可されていない外部APIを追加しない。
- コードの単体テストと分類改善の実験を区別する。「必ず正解」「17件合格なので未知特許も合格」と書かない。
- 実環境が未接続・資料不足・予算超過なら、具体的な不足箇所を残して inconclusive とする。正解や実行結果を作らない。
