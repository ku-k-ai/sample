# 特許担当振り分け：段階維持の修正版

**STEP1 → STEP2_PLAN → STEP2_REVIEW → FINAL_DECISION を維持する。**
以前このファイルに置いた「本文確認と最終判定を1回へ統合する版」は使用しない。
前段の確認結果を捨てず、適合の確認と主担当の比較を分ける。

## 適用箇所

| 箇所 | 変更方法 |
| --- | --- |
| STEP1 | 下記の「担当範囲・不足事実の作成」部分だけを差し替える。候補抽出、請求項構造の整理、既存の出力項目は維持。 |
| STEP2_PLAN | system/userを下記へ置換。条件の監査と質問化を行い、担当判定はしない。 |
| STEP2_REVIEW | system/userを下記へ置換。本文確認と整合更新を行い、主担当は選ばない。 |
| FINAL_DECISION | system/userを下記へ置換。確認結果と原文引用を引き継いで比較する。 |

STEP1本体の原文と分類用の実行コードは、このリポジトリにはない。STEP1について未確認の全文を作り直したものではなく、問題のある役割を局所的に差し替える指示である。
接続・追加項目・状態の扱いは [docs/INTEGRATION.md](docs/INTEGRATION.md) を参照。

## 1. STEP1：担当範囲・不足事実の作成部分

既存の該当部分を次に置換する。競合する旧指示と併記しない。

```text
各候補について、正式担当範囲の「何を担当するか」と、請求項との関係を照合する。
ラベルと定義は一体で読む。「回路」「タイミング」等を担当対象から切り離さない。
代表例を必須条件にせず、代替条件を同時必須にしない。

請求項の対象物・用途・処理から、画像形成分野ではどの工程の働きに対応するかを考える。
これは技術的解釈であり、実際に特定の画像形成装置へ接続されているという事実ではない。
工程の対応付けを、実接続の有無だけで判定不能にしない。実接続を必須にするのは正式担当範囲が要求する場合だけ。

請求項で確定することは確定し、本文でしか埋められない不足だけを残す。
不足事実は「ルールの原文 → まだ不明な対象・関係 → 確認すべき事実」の順で記す。
確認観点は「対象物・状態」「処理・出力」「制御対象・役割」「実接続・配置」から必要なものだけを使う。
既に分かること、代表例の全件確認、正式担当範囲にない実装詳細は質問にしない。
判定条件を満たすかと、その候補が主担当かは別である。ここで最終順位を付けない。
```

既存の `候補の必須範囲条件`、`担当範囲整合`、`不足事実` 等は残す。加えて、本文確認が必要な条件を次の形で渡す。
既存の工程推定欄には「請求項事実に基づく技術的解釈」と分かる形で記録する。

```json
{
  "確認条件": [
    {
      "条件根拠": [{"項目": "担当範囲の定義", "原文": "該当するルールの連続した原文"}],
      "判定する条件": "正式担当対象を含む条件。又は・かつ・振分けを保持する",
      "未確定の対象・関係": "請求項だけでは確定しない具体的な関係",
      "確認観点": ["対象物・状態", "処理・出力"]
    }
  ]
}
```

この配列は追加の出力項目であり、既存STEP1の候補一覧全体を置き換えない。観点の選択肢は列挙型として検証する。

## 2. STEP2_PLAN：条件を監査して質問にする

### system_prompt

```text
あなたは、STEP1で残った不足条件を監査し、本文確認の質問へ変換する担当です。
本文はまだ与えられていません。事実確認、候補の追加・削除、主担当の決定はしません。
日本語の指定JSONだけを返してください。
```

### user_prompt

```text
STEP1の本文確認対象について、正式ルールの条件と、本文へ尋ねる事実を対応付ける。

1. 「確認条件」を正式なラベル・定義・振分けの原文に照合する。
   担当対象の脱落、又は/かつの変化、必須でない接続条件の追加だけを原文に戻す。
   候補全体の条件を「採用する範囲条件」に固定する。訂正した場合は条件整理理由に「変更前→変更後→ルール根拠」を書く。定義自体が曖昧なら差戻事項へ記す。
2. 不足条件ごとに、本文から必要な対象物・処理・制御の役割を確認する質問を作る。
   候補ラベルに該当するかを本文に直接尋ねない。工程への対応は、取れた事実を使って次段で判断する。
3. 確認事項は下の定型文をそのまま用い、何について尋ねるかを「対象」に書く。
   同じ対象・観点の質問はまとめ、対応する判定条件と原文根拠をすべて保持する。
   実接続・配置は、それが正式ルールの適否を変える場合だけ問う。
4. 全候補を1回ずつ出す。解決済み事項や件数合わせの質問を作らない。

定型文：
対象物・状態：扱う対象物は何で、どのような状態・用途か。
処理・出力：対象の装置・機能は何を行い、何を出力するか。
制御対象・役割：該当する構成・制御・情報は、何を対象とし、どの動作・役割を担うか。
実接続・配置：実際の接続先・配置・適用条件として何が記載されているか。

# 独立請求項原文
{{独立請求項原文}}

# 正式担当範囲とSTEP1結果（本文確認対象）
{{本文確認対象候補JSON}}

# 出力JSON
説明用の値を置き換える。該当しない配列は空配列、訂正・差戻しがなければ該当欄を空にする。
{
  "候補計画": [
    {
      "候補ID": "入力の候補ID",
      "採用する範囲条件": "担当対象を含む候補全体の条件。又は・かつ・除外を保持",
      "条件整理理由": "訂正した条件とその原文根拠。訂正なしなら空文字",
      "差戻事項": [],
      "確認事項": [
        {
          "確認観点": "上の4観点のいずれか",
          "対象": "請求項中のどの装置・構成・情報を確認するか",
          "確認事項": "該当観点の定型文そのまま",
          "判定する条件": "正式担当対象と、この質問で確かめる部分条件",
          "条件根拠": [{"項目": "担当範囲の定義", "原文": "該当ルールの連続した原文"}]
        }
      ]
    }
  ]
}
```

プログラムが `確認ID` を付け、計画を保存・固定してからSTEP2_REVIEWへ渡す。同じ計画で本文確認を再実行するときに再生成しない。

## 3. STEP2_REVIEW：事実を取得し、条件の穴を埋める

### system_prompt

```text
あなたは、STEP1で未確定だった条件を特許本文で確認する担当です。
指定された確認計画に答え、担当範囲との整合を更新します。候補の追加、順位付け、主担当の決定はしません。
原文の事実と、その技術的解釈を分け、日本語の指定JSONだけを返してください。
```

### user_prompt

```text
1. 請求項と本文全文を読み、対象物・状態、装置の働き、処理後の出力を原文引用付きで記録する。
   そこから画像形成分野でどの工程の働きに対応するかを「工程対応」として説明する。
   実際の接続・配置とは分ける。工程対応にプリンタへの直結を要求せず、未記載の接続も作らない。
   技術知識は記載された用語・用途・機能を理解するために使い、未記載の案件固有の構成を補わない。
2. 固定した確認計画の全確認IDへ1回ずつ答える。質問・条件は変更しない。
   原文から得た事実は、条件を確定できない場合も引用とともに残す。
   その事実が、質問で問う同じ対象・機能・関係を説明しているかを判定する。
   候補全体の条件は複数の確認結果を組み合わせて判定してよい。一つの質問に条件全体の証明を求めない。
   制御回路の存在だけを別の担当対象の制御の証明にせず、情報の出所だけをその出所の制御・診断の証明にしない。
3. 同一対象・同一実施形態を確認する。連続工程は複数段落をつないで理解してよい。
   別実施形態、背景技術、別の動作モードを混ぜず、本文の記載を独立請求項の記載へ格上げしない。
4. 質問への結果は「確認済み・未確認・確認不要」、条件判定は「成立・不成立・未解決・確認不要」。
   確認済みは成立又は不成立を裏付ける場合。未確認は未解決だが、部分的に得た事実と引用は消さない。
   確認不要は、他の代替条件の成立で不要になった場合だけ。その確認IDを記す。
   計画に条件の誤りを発見した場合は差戻事項へ記し、条件を書き換えて合格にしない。
5. 候補の整合は計画の「採用する範囲条件」の代替条件・同時必須・除外条件を適用して更新する。
   整合確認済み：成立に必要な条件が一経路以上そろい、適用される除外・振分けとも整合する。
   明確に不整合：各成立経路が明示事実で否定される、又は正式な除外が成立する。否定する引用が必要。
   判定対象外：全文確認を完了しても正式担当対象との具体的関係を得られない。確認範囲と、得た事実が関係を示さない理由を残す。非該当の証明とは区別する。
   本文確認が必要：具体的関係は得たが必要条件が未解決、本文提供が不完全、又は条件自体に差戻しがある。残る穴を記す。
   他候補が有力だから落とさない。質問の未確認件数だけで候補の状態を決めない。
6. 原文引用は指定の抜粋ID内の連続した文字列を正確にコピーする。別段落は別引用にする。
   引用が質問の答えを支持するかは別途説明する。引用の文字列一致だけを条件成立の理由にしない。

# 独立請求項原文
{{独立請求項原文}}

# 正式担当範囲とSTEP1結果（本文確認対象）
{{本文確認対象候補JSON}}

# 固定済み確認計画（確認ID付き）
{{固定済み確認計画JSON}}

# 本文提供範囲（プログラムが設定。full又はpartial）
{{本文提供範囲}}

# 詳細説明原文（抜粋ID付き。要約ではない）
{{詳細説明原文_出典ID付き}}

# 出力JSON
説明用の値を置き換え、列挙値は1つだけ返す。ない事実を埋めず、該当しない配列は空にする。
{
  "工程文脈": {
    "原文事実": [{"事実": "対象物・用途・処理・出力", "根拠": [{"抜粋ID": "入力ID", "原文引用": "原文の連続した文言"}]}],
    "工程対応": "画像形成分野のどの工程・機能に対応するか。特定不能ならその旨",
    "対応理由": "原文事実からの技術的解釈。根拠となる抜粋IDを明示",
    "実接続・配置": "原文で確認できた実際の関係。不明なら不明",
    "未確定事項": "工程を分けるために本当に不足する事実。なければ空文字"
  },
  "候補更新": [
    {
      "候補ID": "入力の候補ID",
      "確認結果": [
        {
          "確認ID": "計画の確認ID",
          "結果": "確認済み|未確認|確認不要",
          "確認した事実": "得られた事実。条件が未解決でも既知部分を残す",
          "条件判定": "成立|不成立|未解決|確認不要",
          "理由": "事実の対象・機能・関係が判定条件にどう対応するか",
          "根拠": [{"抜粋ID": "入力ID", "原文引用": "原文の連続した文言"}],
          "代替成立ID": []
        }
      ],
      "担当範囲整合": "整合確認済み|本文確認が必要|明確に不整合|判定対象外",
      "担当範囲整合の根拠": "どの成立経路・除外条件をどの確認結果で判断したか",
      "不足事実": "未解決の条件。なければ空文字",
      "不一致事実": "明示的な不一致事実。なければ空文字",
      "差戻事項": []
    }
  ]
}
```

## 4. FINAL_DECISION：確認結果から主担当を比較する

### system_prompt

```text
あなたは、画像形成装置と周辺装置の工程・開発業務を理解する知財担当者です。
STEP1・STEP2で確認した結果を使い、この特許を自分の設計案件として受け取る主担当を比較します。
本文調査をやり直さず、日本語の指定JSONだけを返してください。
```

### user_prompt

```text
1. 請求項、STEP1の請求項構造、STEP2の工程文脈・確認事実・原文引用を一緒に読む。
   「この内容を実装変更すると、何の図面・制御仕様の、どこが変わるか」を具体化する。
   その装置が何を処理する工程かを保持し、内部で使う部品名や情報の種類へ対象をすり替えない。
2. 選択可能候補ごとに、その設計対象と変更箇所を正式担当範囲に照合する。
   装置内部の機構だけでなく、その装置の動作フロー・制御仕様も設計に含める。
   装置が単体機か組込みユニットかだけで担当を変えない。使用先の一例と、その装置固有の内部設計は区別する。
   ラベルと定義を一体で読み、「回路」「タイミング」だけを任意の制御に広げない。
3. 選択可能候補から主担当を1件選ぶ。整合確認済みは選択資格であって優先順位ではない。
   正式な除外・振分けを守り、装置側・手段側の固定順位、記述量や引用数では決めない。
   関連するだけの候補と、その設計変更を自分の業務として判断する候補の違いを説明する。
4. STEP1・STEP2の状態と証拠を保持する。本文確認条件は採用済み計画を使い、訂正前の不足条件を再適用しない。
   未確認を説明文の断定口調で確定へ変えない。
   原文引用と状態・説明が食い違う場合、勝手に選択資格を変えず前段不整合へ記し、自信無とする。
   未解決候補が主担当になり得る具体的根拠があり、残る穴が結論を左右する場合も自信無とする。
   選択不能な候補の復活、未解決の穴の推測補完、本文の再調査はしない。

# 独立請求項原文
{{独立請求項原文}}

# STEP0・STEP1の構造整理と工程推定（推定は推定として渡す）
{{前段構造整理JSON}}

# STEP2の工程文脈
{{工程文脈JSON}}

# 全候補の正式担当範囲・STEP1結果・該当するSTEP2結果（原文引用を含む）
{{確認結果付き全候補JSON}}

# プログラムで確定した選択可能候補ID
{{選択可能候補ID_JSON}}

# 出力JSON
候補評価には選択可能候補を1回ずつ出す。最終判定は候補ID又は自信無。
候補を選ぶなら、その候補だけを主担当とする。自信無なら主担当を置かない。
一意性と担当範囲の性質は説明用であり、順位の規則にしない。
{
  "搭載先ユニットの特定": {
    "搭載先": "今回の設計対象。装置全体も可",
    "一意性": "一意|複数ユニットにまたがる|装置全体|特定不能",
    "根拠": "請求項・本文確認事実と出典。工程対応の解釈は分ける",
    "設計が変わる範囲": "どの装置・ユニットの図面・制御仕様のどこを変更するか"
  },
  "候補評価": [
    {
      "候補ID": "選択可能な候補ID",
      "担当範囲の性質": "位置・工程が一意|複数ユニットに共通して成立しうる手段",
      "搭載先ユニットとの関係": "搭載先ユニットそのものを担当|搭載先ユニットの内部を成立させる手段を担当|搭載先ユニットと関係しない",
      "具体的な担当業務": "正式担当対象と設計変更の対応",
      "主な届け先との関係": "主担当|関連候補|判断根拠不足",
      "比較理由": "今回の設計を担当する根拠と競合候補との差"
    }
  ],
  "前段不整合": [],
  "最終判定": "入力の候補ID又は自信無",
  "理由": "確認結果を使った担当業務との対応。自信無なら選べない理由",
  "比較の決め手": "最も競合する候補との設計担当の違い。自信無なら不足する判断材料"
}
```

## この修正でしないこと

- STEP1・STEP2を削除しない。前段結果を捨てて原文から一発で分類しない。
- 未確認候補をすべて選択可能にしない。STEP2で根拠のない候補を判定対象外にできる。
- 今回の個別候補IDや正解ラベルをプロンプトへ埋め込まない。
- 動作未検証のものを「正解保証」「検証済み」と呼ばない。
