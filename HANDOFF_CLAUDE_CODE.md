# Claude Codeへの引き継ぎ：特許の届け先分類（2026-09-25 夕方時点）

このファイルを最初に読んでから作業を始めること。前の会話（claude.aiのチャット）の要点をまとめたもの。

## 1. 何をしているか

社内の特許振り分けパイプライン。1件の特許を、どの開発担当（ラベル）に最初に読ませるかを決める。
流れは STEP0 発明理解 → STEP1 請求項と候補の整合 → STEP2（PLAN／REVIEW）本文確認 → 最終判定。
領域は3つ。P（P1〜P6）、E（①〜⑭）、M（給紙、後搬送、後処理、組合せ本体など）。
本番モデルは gpt-5.6-luna（xhigh）。社内の作業・実行記録は `out/llm_eval/` にある。

- GitHub：`ku-k-ai/sample`（**公開リポジトリ**）。検証コード・雛形・合成テストだけを置く。
- 会社のCodexは、このリポジトリの指示書（`CODEX_START*.md`）を読んで作業する。

## 2. 今日の期限とゴール

- **今日が検証の期限。** 次は、今の仕組みを本当の本番に入れて仮本番の運用を始める。
- 本番で完全正解は求めない。求めるのは、**明らかに違う分類（人間が思いつきもしない届け先）を出さないこと**。
  - 例：M_15（気流の特許）の後搬送は「ありえない」誤り。
  - P6とP2の取り違えのような、もっともらしい誤りは許容範囲。
  - E_4の定着制御も許容（ベストは⑧シート搬送制御）。

## 3. 今の状態

| 版 | 中身 | 17件の結果 | 扱い |
|---|---|---|---|
| v4（今朝08:35の本番） | `prompts/final_decision_v4.txt` にCodexの追記あり | 12/17（E_4許容で実質13） | 仮本番の基準版 |
| Bの段落の差し替え（14:12） | 「直接構成・変更する仕組み」で1段に止める | 11/17 | **不採用**。本番テンプレートから戻す必要あり |
| v4.2（未検証） | v4の手順3に1行追加 | 本番モデルでは未実行 | 今日これを検証して採否を決める |

v4での外れと、許容の扱い：
- P_0：P2であるべきところがP6（もっともらしい誤り）
- P_9：P6であるべきところがP1／P2（もっともらしい誤り）
- M_15：組合せ本体であるべきところが後搬送（**ありえない誤り**。これが本題）
- M_16：自信無（人に回るので可）

## 4. v4.2の中身と根拠

手順3の「特許本文での装置の呼び名や区分け…」の行の直後に、次の1行だけを足す。

> 代表例（非限定）の欄に、特徴部分の仕組みそのものの名前（部品の名前ではなく、機構・流れ・配置の名前）がそのまま挙がっている項目は、その仕組みを受け持ちます。担当領域が技術・残余に見える項目でも、この場合は「受け持つ」とし、技術担当として外さないでください。

- **根拠（出力から確認済み）**：M_15が外れた回は、モデルが組合せ本体の代表例に「エアフロー」があると自分で書いたうえで、組合せ本体を「技術」に分類し、「担当ユニットがあれば技術担当は選ばない」規則で外していた。当たった回は、同じ組合せ本体を「残余・受け持つ」にしていた。
- **影響範囲**：EとPのルール表は代表例の欄が空なので、構造上Mにしか効かない。
- **事前確認**（Sonnet、Mの8件×3回、誤りを誘う本文引用入り）
  - M_6は3/3で後搬送
  - M_15の後搬送は0/3（本体1、読取光学系2）
  - 封入・本体2件は維持
  - EとPの抜き取り（CIS、LED、光輝性）は変化なし
- **残る弱点**：M_15が読取光学系に行く回がある。許容できるかはユーザーが判断する。

## 5. 次にやること（順番どおり）

1. **v4.2の一式を、リポジトリの新しいブランチに入れてpushする。** `v4_2.patch`（同梱）を `git am` するか、zipの3ファイルを置く。そのあと `python -m unittest discover -s tests -p "test_pipeline_guards.py"` で39本がすべて通ることを確認する。
2. **Codexに `CODEX_START_v4_2.md` を読ませる。** 中身はこの順番。
   - 本番テンプレートを今朝の版に戻す（14:12のBの段落を消す）
   - v4.2の1行を足す
   - Mの8件を、STEP2固定の条件で検証する：08:35の入力で2回、14:12の入力で1回
   - 実行前に固定した合否の基準で判定する
     - M_15で後搬送が1回も出ない
     - M_6が全回後搬送
     - 他のM件は今朝と同じか自信無
3. **合格ならv4.2で仮本番、不合格なら今朝のv4で仮本番。** どちらでも今日中に決める。

## 6. ユーザーの方針（必ず守る）

- **過適合は禁止。** 17件の正解に合わせて、特定の特許の対象物の名前をプロンプトに入れない。原則は、特定の件ではなく「なぜそうなるか」から説明できるものだけにする。
- **説明できないことは言わない。** 正解・不正解のどちらについても、「なぜモデルがそう判断したか」をプロンプトと入力から説明する。1回当たっただけでは効果とみなさない。
- **正式ルール表に列を足さない。** 他部門にも使う仕組みだから。
- **ルールベースの判定コードは使わない。** 判定はLLMが行う。検査（guards）はコードで行ってよい。
- **STEP1/2の表は外さない。** 最終判定は、表で残った候補から選ぶ。
- **ユーザーが納得していない案を、勝手に大量に回してトークンを使わない。** 回す前に、狙いと予想を短く示して了承を得る。
- **回答は短く、結論から。** ユーザーは長文を読まない。
- **「簡単に戻せ」のような軽い言い方をしない。** 戻すなら、そう判断した理由（切り分けの結果）を添える。
- 公開リポジトリに、社内ルール表・特許の入力・送受信ログ・正解を置かない。旧版の `code/rules_E.json` と `verify_CIS01/` がまだ公開されているので、消すかどうかをユーザーに確認する。

## 7. 今日わかったこと（再発防止）

- **1回ずつの実行では判断できない。** 同じプロンプトでも、STEP2を流し直すと最終判定に渡る引用が変わり、答えが入れ替わる。M_15では、5件の検証（STEP2固定）では引用に気流の構造【0084】【0094】があって本体、全件（STEP2再実行）では検査から排出の経路【0091】【0075】だけになって後搬送だった。判断は、同じ入力で複数回流した結果か、入力を固定した比較でする。
- **不採用にした案（再提案しない）**
  - Bの段落を1段で止める：E_3が低圧電源、M_6が本体に崩れた。
  - 目的が複数ユニットにまたがれば本体共通：M_6が本体に崩れた。
  - 発明の説明を固定の入力にし、背景技術の用途を理由にさせない：16件中8件に悪化した。
  - 特徴部分と定義の語句を一字一句照合させる：自信無が増えた（7/16）。
  - 過去の判定をRAGで使う：以前に試して、過去判定の質が低いのでやめた。
- **次の改善候補（仮本番が始まってから）**
  - 最終判定に、候補に左右されない発明の説明を渡す。ただし、背景技術の扱いの文は付けない。
  - P6の定義を、括弧書き（画像処理に関連する分野）ではなく、列挙された仕事で読ませる。P_0とP_9が逆向きに外れている原因。
  - US/EPは未検証。背景技術がシステムの列に入らない問題があり、要約で代用すると悪化した。
