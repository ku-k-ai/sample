# Codexへの作業指示：v4.3（2026-09-25）

変えるのは最終判定だけです。分類ルール表（ドメイン側の管理物）、STEP0〜STEP2、検査コードは変えません。

- **A**：「何のための構成か」を、STEP2の引用から答えさせない。独立請求項と、特許自身の【発明が解決しようとする課題】【発明の効果】から答えさせる（プロンプト3か所、コード1か所）。
- **C**：v4の「定義は例で非限定」の行に、広げてよい範囲の限度を1行足す（プロンプト1か所）。

## 0. 何を直すのか（根拠）

- **M_6（正解：後搬送）の答えが回ごとに変わる。** 08:35は後搬送、14:12は組合せ本体、17:45は給紙。プロンプトはどれも同じv4。
  - STEP0は「シートトレイ及び再搬送部の引出し構造」と、両方を同じ重みで挙げている。請求項は、壁の位置をフィードローラ（給紙側）との関係でも決めている。
  - 人は、取手がシュート（第2アウターフレーム）のもので、壁が取手とシュートの引き出し方向（第2方向）を基準にしていることから、後搬送と読む。v4にも「位置関係の基準として出てくるだけの部品は担当ユニットにしない」とある。
  - 17:45のモデルは、STEP2が拾った実施例【0074】（シートトレイ引き出し時のフィードローラとの干渉回避）を「何のための構成か」の答えにして、給紙へ行った。STEP2の引用は候補を確かめるために集めたものなので、回ごとに中身が変わる。これが答えが回ごとに変わる原因。
  - 課題【0006】（シュートを引き出すための開口からの音漏れ抑制）は、シュート側であることを示す。Claude側の事前確認（Sonnet、各1回）では、同じv4で、請求項だけを渡すと給紙、請求項＋課題〜効果を渡すと後搬送だった。STEP2の引用を外すだけでは足りず、課題・効果の入力が効いている。
- **P_0（正解：P2）が9/10は当たり、今はP6。**
  - 9/10の出力は、P6を「センサー等を用いる正式担当業務との関係が未確認」として外していた。
  - 今のv4の出力は、P6の定義の括弧書き「（画像処理に関連する分野）」を根拠に、印刷データの変換をP6に入れている。
  - v4で足した「定義や代表例は非限定」の行が、括弧書きのまとめまで広げて読ませている、と考えられる。Cはその限度を決める。
- **背景技術と【課題を解決するための手段】は渡さない。** 背景技術には周辺の用途（例：読取部は画質判定に用いられる）が書かれていて、判定を後搬送に引っ張った。手段には従属項の細部（例：フィードローラとの干渉回避）が並んでいる。

## 1. コード（A）

1. `patent_pipeline/sections.py` の `extract_problem_and_effect(full_text)` を、本番の最終入力を組み立てる処理から呼ぶ。引数は「詳細な説明」の全文。
   - 戻り値は `[{"見出し": "発明が解決しようとする課題", "原文": ...}, {"見出し": "発明の効果", "原文": ...}]`。見出しが無ければ `[]`。
2. 戻り値のJSONを、最終判定テンプレートの `{{PROBLEM_AND_EFFECT_JSON}}` に差し込む。
   - このリポジトリの `render_final_prompt(..., problem_effect=...)` が参考実装。本番で別の関数を使っているなら、その `.replace` の並びに1行足す。
3. **確認**
   - 17件すべてについて、切り出した見出しと文字数を一覧にする。空の件があれば報告する。
   - US/EPの「詳細な説明」の列に、課題・効果にあたる見出しがあるかを調べて報告する。無ければ `[]` になり、その件の動きはv4と同じになる。

## 2. 本番の最終判定テンプレート（A、3か所）

本番テンプレートは丸ごと置き換えない。各行は、置き換え前の文が本番に**ちょうど1回**あることを確かめてから変える。見つからない・2回以上あるなら、止めて報告する。

### 2-1. 手順2の「B のとき」の行の直後に1行足す
目印（この行は変えない）：
```
- B のとき：特徴部分が「何のために設けられているか」で決めます。その構成が支えている機能・操作・紙の流れを受け持つユニットが担当ユニットです。
```
直後に足す行（先頭は半角スペース2つ＋「- 」）：
```
  - 「何のために設けられているか」は、「発明の課題と効果」に書かれた目的から答えてください。そこに書かれた目的をそのまま使い、その目的がさらに何の役に立つかまではさかのぼらないでください。「発明の課題と効果」が空のときは、独立請求項から答えてください。
```

### 2-2. 「STEP0〜STEP2の結果の使い方」の最初の行を置き換える
`- STEP2の本文確認結果・根拠引用は、` で始まる1行を、丸ごと次の文にする：
```
- STEP2の本文確認結果・根拠引用は、請求物や特徴部分がどのユニットに属するかの根拠として使ってください（例：「画像読取り用の機能ユニットとして記載されている」と確認されていれば、担当ユニットの根拠になります）。「何のための構成か」の答えには使わないでください。STEP2の引用は候補ごとの確認のために集めたもので、実施例の周辺の動作が含まれるためです。
```

### 2-3. 入力欄を足す
`{{INDEPENDENT_CLAIMS}}` の行の直後に、空行を1つ空けて次の2行を足す：
```
# 発明の課題と効果（この特許自身の記載。候補によらず毎回同じ）
{{PROBLEM_AND_EFFECT_JSON}}
```

確認：変更前との `diff` で、`<` が1行（2-2の元の行）、`>` が2-1・2-2・2-3の行だけになっていること。参考として、このリポジトリの `final_decision_v4.txt` と `final_decision_v4_3.txt` の差分も同じ形になっている。

## 3. 手順3に1行足す（C）

目印（この行は変えない）：
```
  - 定義や代表例に挙がっている装置名は例です。代表例は非限定です。担当ユニットの名前が定義に無いことだけで「受け持たない」にしないでください。定義が「〜などの装置」と書いていれば、挙がっていない装置でも、同じ仕事をする装置はその範囲に入ります。
```
直後に足す行（同じ字下げ）：
```
  - 範囲を広げてよいのは、定義・代表例に挙がっている仕事と同じ種類の仕事までです。括弧書きのまとめ（「〜に関連する分野」など）だけを根拠に、挙がっている仕事と種類の違うものまで範囲に入れないでください。
```
この目印の行は、封入装置（後処理）とカッター（①オプション制御）を当てている行。Cで崩れないかを4の検証で必ず見る。

## 4. 検証（最終段だけ。入力は17:45の保存済みSTEP0〜STEP2で固定）

| 条件 | 対象 | 版 | 回数 |
|---|---|---|---|
| 対照 | 17件 | 現行本番 | 17:45の結果＋2回 |
| A | 17件 | 2のテンプレート変更＋1の入力 | 3回 |
| A+C | 17件 | Aに3の1行を加えたもの | 3回 |

- モデル・推論設定は本番と同じ。自動再試行・回答修復は使わない。出力検証は通す。

### 合否の基準（実行前に固定。結果を見てから変えない）

**Aを採用する条件**（全部満たすこと）
1. Eの5件：3回すべてが17:45の本番と同じラベル。
2. 対照で3回中2回以上正解の件が、Aで3回中2回以上不正解にならない。
3. 対照で一度も出ていないラベルがAで出た件は、一覧にしてユーザーに見せる（採否はユーザーが決める）。

**Cを採用する条件**（Aの採用が前提。全部満たすこと）
1. 封入装置（M_12、後処理）とカッター（E、①オプション制御）：3回すべて正解。
2. Aで3回中2回以上正解の件が、A+Cで3回中2回以上不正解にならない。

**目標**（採否の条件ではない。報告だけ）
- M_6：後搬送が3回中3回。
- P_0：P2が3回中2回以上。

**今回の対象外**
- M_15（組合せ本体）：ルール表の文面（「装置本体のエアフロー」「エアフローは、場合によって各要素へ」）からは、モデルの読み（使われる要素へ）も成り立ってしまう。表を変えずにプロンプトで直すと、同じ理屈がEの件（CIS→リーダー、カッター→①）を崩す。結果は報告するが、判定には使わない。
- P_9：自信無（人に回る）なら可。

## 5. 報告

`out/llm_eval/v4_3_check_<日時>/README.md` に、次をまとめる。
- 案件 × 条件（対照×3、A×3、A+C×3）× 判定の表
- 変わった件の「何のための構成か」と「担当ユニットの根拠」
- 1-3の切り出し一覧と、US/EPの調査結果

## やってはいけないこと

- 検証の途中でプロンプトを直さない（直すなら、検証をやり直す）。
- 特定の特許の対象物の名前を、プロンプトに入れない。
- 分類ルール表（ドメイン側の管理物）を変えない。列も足さない。
- このリポジトリ（公開）に、社内ルール表・特許の入力・送受信ログ・正解を置かない。

## 運用で推奨（任意）

最終判定だけを2回流し、判定が割れた件は「要確認」として人に回す。プロンプトは変えず、コードで判定する。
